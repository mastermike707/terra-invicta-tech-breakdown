---
trigger: always_on
---

Web Preview Policy:
Assume the local server is already running. DO NOT execute commands to start one. NEVER open local file:/// paths. Use the browser subagent exclusively to navigate to http://localhost:3000 for all UI testing and verification.
